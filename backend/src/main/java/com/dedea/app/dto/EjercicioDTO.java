package com.dedea.app.dto;

// Item de la lista "Curso". Sin contenido generado todavía (eso solo se pide al abrir uno).
public record EjercicioDTO(
        Integer id,
        String titulo,
        String descripcion,
        String tipo
) {}
